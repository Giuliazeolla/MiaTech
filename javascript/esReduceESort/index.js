const spese = [100, 200, 150, 50, 300];

const totaleSpese = spese.reduce((totale, spese) => totale + spese);

const words = ["banana", "apple", "cherry", "date"];

words.sort();

console.log(totaleSpese);
console.log(words);